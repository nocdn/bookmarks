# SUPABASE_URL:  found in your Supabase project settings, looks like https://xyzcompanysupabase.supabase.co
# SUPABASE_KEY:  use the anon-public key for client-side apps, or the service-role key if this API will run only on the server.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your_anon_or_service_role_key_here

GEMINI_API_KEY=your_gemini_api_key_here

# Use "*" to allow any origin (NOT recommended in production). 
ALLOW_ORIGIN=*

# Flask server options
PORT=4871